# demo/ 中等可靠性改造任务规划

## 目标与拆分原则

本任务规划基于 [需求说明.md](/Users/ganyu/project/work/songguoyun/candidate-pack/需求说明.md) 与 [方案文档.md](/Users/ganyu/project/work/songguoyun/candidate-pack/方案文档.md)，面向当前 `demo/` 原型代码，输出一组可独立开发、可独立测试、可独立提交 PR 的改造任务。

拆分原则如下：

- 先稳住一对一消息主链路，再扩展到投影一致性与多端同步。
- 先解决 P0 一致性问题，再处理 P1 可见性与维护性问题。
- 一个任务只围绕一个核心业务目标展开，避免多个任务同时改同一段状态机逻辑。
- 每个任务必须可以单独验证，不依赖“所有任务一起完成后才知道是否正确”。
- 每个任务都要能形成独立 PR，且 PR 合入后系统仍保持可运行。

## 推荐实施顺序

1. 消息发送幂等控制
2. 发送状态机收敛与回退保护
3. 最近会话与未读真相重算
4. 多设备同步确认式游标
5. 兼容逻辑收敛与排障能力补齐

这个顺序的原因：

- 任务 1 和任务 2 先解决“消息重复”和“状态回退”这两个最核心的 P0 风险。
- 任务 3 在主状态机稳定后，再处理 summary/unread，避免投影反复重做。
- 任务 4 单独处理 sync cursor 语义，和会话投影解耦，便于独立验证多端一致性。
- 任务 5 最后做兼容收敛、日志和回归加固，避免过早修改辅助逻辑干扰主链路判断。

## 当前开发进度

- `已完成` 任务 1：消息发送幂等控制
- `已完成` 任务 2：发送状态机收敛与回退保护
- `已完成` 任务 3：最近会话与未读真相重算
- `已完成` 任务 4：多设备同步确认式游标
- `已完成` 任务 5：兼容逻辑收敛与排障能力补齐

本轮已落地内容：

- 发送链路已接入 `ClientMsgID` 幂等控制，并为缺失 `ClientMsgID` 的兼容设备保留短窗口兜底去重。
- attempt 主状态机已收敛到 `ActiveAttemptID` 语义，旧回调和重复回调不会再覆盖当前消息状态。
- retry 已改为原子语义，避免 `SaveMessage + StartAttempt` 分步更新导致的中间态污染。
- summary / unread 已切换到基于消息真相和 `LastReadMessageID` 的局部重算，不再依赖脆弱的增量累加。
- sync cursor 已切换到“ack 后推进”语义，`next_cursor` 不再被自动持久化为设备进度。
- 已将 `LegacyStatus` 限定为列表展示兼容层，不再影响消息主状态、summary 真相或 unread 真相。
- 已补充主链路排障日志，关键路径可串起 request、user、conversation、message、attempt、device、cursor。
- 已补充自动化测试，覆盖重复发送、兼容去重、stale callback、重复 callback、retry 冲突、summary 可见性、mark read、sync ack、legacy display status 边界等场景。
- 已根据代码审查继续完成一轮生产化加固：
  - 发送幂等已收敛到 repository 原子边界，补齐并发发送去重测试
  - `MarkConversationRead` 已进入 sync 事件流，补齐跨端已读同步测试
  - 详情列表已按用户可见性过滤，避免 receiver 看到未 sent 消息
  - summary/read state key 已从不稳定的 rune 拼接改为稳定字符串键

---

## 任务 1：消息发送幂等控制

## 当前状态

- `已完成`

## 任务目标

- 解决用户因为重复点击、网络抖动、客户端重放导致的一次发送生成多条消息的问题。
- 收敛当前“发送请求即直接建消息”的行为，让 `ClientMsgID` 正式成为主幂等键。
- 这是 P0 风险，直接影响聊天详情页可见结果，也是后续所有状态一致性治理的基础。

## 前置依赖

- 无。
- 这是整个方案的起点，因为不先稳定消息创建语义，后续 retry、summary、sync 都会建立在不可靠的数据上。

## 任务边界

- 只处理“是否创建新消息、是否复用已有消息”的判断。
- 不在本任务中处理旧回调覆盖新状态。
- 不在本任务中处理 summary/unread 重算。
- 不在本任务中改 sync cursor 语义。

## 业务范围

- 发送主链路。
- 兼容设备在缺失 `ClientMsgID` 时的兜底去重。
- 已存在消息返回时，如何返回当前有效 attempt。

## 主要改造点

- 将发送幂等主键定义为 `senderID + conversationID + clientMsgID`。
- 在 repository 中建立稳定的 `clientMsgIndex`。
- `FindByClientMsgID` 升级为面向会话的精确幂等查询，而不是只按 `senderID + clientMsgID`。
- `SendMessage` 在命中幂等时直接返回已有消息，不再创建新 `Message`。
- 仅在 `ClientMsgID` 缺失且设备命中兼容策略时，使用短窗口内容去重作为兜底。
- 返回已有消息时，优先返回 `ActiveAttemptID` 对应 attempt；若不存在，则返回该消息最新 attempt。

## 涉及的一致性问题

- 消息重复。
- retry 前置数据污染。
- 后续 summary/unread 计算基础不稳定。

## 独立验证方式

- 同一 `ClientMsgID` 连续发送两次，只创建一条消息。
- 同一 `ClientMsgID` 连续发送两次，不额外增加 attempt。
- 无 `ClientMsgID` 且非兼容设备时，不进行内容去重。
- 无 `ClientMsgID` 且兼容设备命中兜底策略时，返回已有消息。

## PR 边界建议

- 该 PR 只接受发送入口与消息创建索引相关改动。
- 不接受 retry 状态机、summary/unread、sync cursor 的顺手修改。
- 如果需要补测试，只补发送幂等和兼容兜底场景。

## 风险隔离说明

- 本任务不会修改消息状态推进规则，因此失败时影响面集中在发送创建环节。
- 一旦发现兼容设备兜底命中过宽，可单独回退该策略，不影响主幂等键逻辑。

## 已完成实现

- 已将主幂等键收敛为 `senderID + conversationID + clientMsgID`。
- 已在 repository 中建立消息幂等索引，重复发送直接返回已有消息。
- 已为缺失 `ClientMsgID` 的兼容设备接入短窗口内容去重兜底。
- 已补测试覆盖：
  - 同 `ClientMsgID` 重复发送
  - 无 `ClientMsgID` 的默认设备不去重
  - 无 `ClientMsgID` 的兼容设备兜底去重

---

## 任务 2：发送状态机收敛与回退保护

## 当前状态

- `已完成`

## 任务目标

- 解决旧回调覆盖新状态、重试后旧结果回写、新旧 attempt 互相污染的问题。
- 把 `ActiveAttemptID` 从“记录字段”升级为真正的当前 attempt 语义。
- 这是 P0 风险，直接对应“状态回退”和“重试混乱”。

## 前置依赖

- 依赖任务 1 完成。
- 原因是 retry 和回调处理都需要建立在“同一逻辑消息不会被重复创建”的前提上，否则状态机保护会被重复消息绕开。

## 任务边界

- 只处理 `Message.Status`、`DeliveryAttempt.Status`、`ActiveAttemptID` 三者关系。
- 本任务内可以重构 retry 主流程，但不处理 summary/unread 重算逻辑。
- 本任务不改 sync cursor 语义。

## 业务范围

- provider callback 完成链路。
- retry 主链路。
- stale callback、重复 callback、冲突 callback 的处理策略。

## 主要改造点

- 将 `CompleteAttempt` 改为严格校验当前 `attemptID` 是否等于 `Message.ActiveAttemptID`。
- 对已完成 attempt 的重复回调做幂等处理，不重复改消息、不重复发事件、不重复更新投影。
- 对旧 attempt 的晚到回调，只允许更新 attempt 自身结果，不允许覆盖消息主状态。
- 将 retry 改造成 repository 内原子方法，在一把锁内完成“校验状态 -> 创建新 attempt -> 更新 active attempt -> 更新 message status”。
- 明确 retry 规则：
  - `failed` 可 retry。
  - `sending` 且 active attempt 仍在运行时，重复 retry 直接返回当前运行中的 attempt。
  - `sent` 和 `deleted` 不允许 retry。
- 接收方不应看到发送方 retry 导致的中间 `sending` 状态回跳。

## 涉及的一致性问题

- 状态回退。
- 重试混乱。
- 多端状态抖动。

## 独立验证方式

- 消息成功后，旧 failed callback 到达，不得改回 failed。
- retry 生成新 attempt 后，旧 attempt 成功或失败都不得覆盖新状态。
- 同一 attempt 重复 success callback，只保留一次有效状态推进。
- `sending` 状态下重复 retry，不再创建新 attempt。
- `sent` 消息 retry 返回业务错误。

## PR 边界建议

- 该 PR 聚焦 attempt 状态机，不接受 read state、summary/unread、sync cursor 语义变更。
- 如果需要扩展 repository 接口，只扩展 attempt 相关方法。

## 风险隔离说明

- 这是主链路最敏感任务，必须单独成 PR，避免和投影重算混在一起难以排查。
- 如果本任务出现问题，回滚只影响回调与 retry，不会同时拖累会话列表和多端同步逻辑。

## 已完成实现

- 已将 `CompleteAttempt` 收敛为基于 `ActiveAttemptID` 的主状态推进。
- 已对重复 callback 做幂等处理，避免重复更新消息状态和投影副作用。
- 已对 stale callback 做忽略保护，旧 attempt 无法再覆盖当前消息状态。
- 已将 retry 主流程收敛为 repository 内原子更新，避免中间态暴露给接收方。
- 已补测试覆盖：
  - retry 后旧 attempt 回调晚到
  - 同一 callback 重放幂等
  - `sending` 状态重复 retry
  - `sent` 消息 retry 拒绝

---

## 任务 3：最近会话与未读真相重算

## 当前状态

- `已完成`

## 任务目标

- 解决最近会话最后一条消息、更新时间、未读角标与消息详情页不一致的问题。
- 将现有“增量拍脑袋更新 summary/unread”的方式改为“基于消息真相与 read state 的局部重算”。
- 这是 P1 风险，但它是用户最容易观察到的持续性错误之一。

## 前置依赖

- 依赖任务 2 完成。
- 原因是 summary/unread 必须建立在稳定的消息状态机之上，否则重算得到的结果本身就不可靠。

## 任务边界

- 只处理最近会话 summary、未读数、read state。
- 不处理 sync cursor 语义。
- 不处理发送幂等和 callback 防回退。

## 业务范围

- 会话摘要更新。
- 已读行为。
- 发送方与接收方消息可见性差异。

## 主要改造点

- 将 `ReadState` 正式接入 repository。
- 引入 `LastReadMessageID` 作为未读计算真相。
- 用局部重算替代 `updateSummaryLocked(...incrementUnread bool)` 这类增量逻辑。
- 定义用户可见性规则：
  - 发送方可见 `sending/failed/sent`。
  - 接收方只可见 `sent`。
- `MarkConversationRead` 改为更新 read state 后重算 summary，而不是直接把 `UnreadCount` 清零。
- 会话列表读取和 summary 计算都只扫描单会话消息集合，避免全表扫描。

## 涉及的一致性问题

- 最近会话列表错误。
- 未读数错误。
- 发送方与接收方可见口径不一致。

## 独立验证方式

- 发送方在消息创建后能看到会话摘要中的 sending 消息。
- 接收方只有在消息 sent 后才看到该消息进入最近会话。
- 已读后 unread 归零，再来新消息时按 `LastReadMessageID` 正确增长。
- 状态回退保护生效后，summary/unread 不再出现脏增量。

## PR 边界建议

- 该 PR 聚焦 summary/unread/read state，不接受 sync cursor 改动。
- 如果需要调整列表读取性能，只处理“基于单会话索引读取”的部分，不做与 sync 相关的事件压缩。

## 风险隔离说明

- 本任务以“重算替代增量”为核心，逻辑清晰，便于独立回归。
- 即使后续 sync 任务尚未完成，单端详情页和最近会话的一致性也能先落地。

## 已完成实现

- 已将 `ReadState` 接入 repository 内存存储，用 `LastReadMessageID` 作为未读真相来源。
- 已将会话摘要更新切换为局部重算，不再依赖 `UnreadCount++` 和直接清零的旧逻辑。
- 已明确 sender / receiver 可见性规则：
  - sender 可见 `sending/failed/sent`
  - receiver 仅可见 `sent`
- 已将 `MarkConversationRead` 改为更新 read state 后重算 summary。
- 已补测试覆盖：
  - sender 在消息未 sent 前可见 summary
  - receiver 在消息 sent 后才进入最近会话
  - mark read 后 unread 清零
  - 新消息到达后 unread 按最后已读位置重新计算

---

## 任务 4：多设备同步确认式游标

## 当前状态

- `已完成`

## 任务目标

- 解决设备只收到响应但未真正应用事件时，服务端 cursor 已经前推，导致后续永久漏事件的问题。
- 将当前“返回即推进”改为“确认后推进”，收敛多端同步边界。
- 这是 P0 风险，直接对应“多设备长期不一致”。

## 前置依赖

- 依赖任务 2 完成。
- 原因是 sync 返回的事件必须反映稳定的主状态机，否则先改 cursor 语义也无法保证最终一致。

## 任务边界

- 只处理设备 cursor 的确认语义。
- 不重做事件模型。
- 不引入新传输协议。
- 不在本任务中修改 summary/unread 规则。

## 业务范围

- `/sync` 拉取链路。
- device cursor 存储与推进规则。
- 老客户端兼容。

## 主要改造点

- 在 `SyncRequest` 中增加 `AckCursor`。
- `Sync` 先处理 `AckCursor`，仅持久化客户端已确认应用的 cursor。
- `next_cursor` 只表示“本次返回到哪里”，不再自动写入持久化设备进度。
- 设备起始 cursor 优先使用请求 `Cursor`，否则使用服务器已 ack cursor。
- `AckDeviceCursor` 必须单调前进，不允许回退。
- HTTP `/sync` 增加可选 `ack_cursor` 参数，保持旧客户端兼容。

## 涉及的一致性问题

- 多设备漏事件。
- 设备间长期不同步。
- 轮询场景下 cursor 被过早推进。

## 独立验证方式

- 第一次 `Sync` 返回事件后，不提交 `AckCursor`，再次按旧 cursor 拉取仍能拿到相同事件。
- 提交 `AckCursor` 后，再次拉取不再返回已确认事件。
- 提交更小的 `AckCursor` 不会导致 cursor 回退。
- 两个设备分别维护各自 ack 进度，互不干扰。

## PR 边界建议

- 该 PR 只处理 sync cursor 语义，不接受 summary/unread 和 retry 逻辑改动。
- 如果需要补事件字段，只补支持 cursor 与回放验证所需的最小字段。

## 风险隔离说明

- 本任务单独成 PR 后，多端一致性可以独立灰度和回归。
- 即使该 PR 回滚，也不会破坏任务 1 到任务 3 的消息主链路语义。

## 已完成实现

- 已在 `SyncRequest` 中加入 `AckCursor` 语义，并通过 API 层接入可选 `ack_cursor` 参数。
- 已将 `Sync` 行为改为：
  - 先持久化已确认应用的 `AckCursor`
  - 再按请求 cursor 或已 ack cursor 拉取事件
  - 只返回 `next_cursor`，不自动推进设备进度
- 已新增设备 cursor 的单调 ack 保护，避免回退。
- 已补测试覆盖：
  - 未 ack 时重复拉取仍返回相同事件
  - ack 后不会再重放已应用事件
  - stale ack 不会导致设备 cursor 回退

---

## 任务 5：兼容逻辑收敛与排障能力补齐

## 当前状态

- `已完成`

## 任务目标

- 在前四个任务完成后，对历史兼容逻辑做作用域收敛，避免再次污染主状态机。
- 同时补齐日志与回归测试，让线上问题可定位、后续改动可守护。
- 这是 P1/P2 风险，不是最先处理的业务错误，但对长期稳定交付非常关键。

## 前置依赖

- 依赖任务 1、任务 2、任务 3、任务 4 完成。
- 原因是只有主行为确定后，才能明确哪些兼容逻辑应该保留、哪些应该降级为旁路参考。

## 任务边界

- 不改变前四个任务已经确定的业务语义。
- 只收敛兼容逻辑的使用边界，补日志、补回归、补注释。
- 不做大规模历史代码清理，不做架构重构。

## 业务范围

- `LegacyStatus` 的展示兼容边界。
- compat helper 的兜底作用域。
- 主链路结构化日志。
- 关键回归场景测试矩阵。

## 主要改造点

- 明确 `LegacyStatus` 只影响部分列表展示，不再参与主状态机、summary/unread、sync 真相判断。
- 明确 `FindLikelyDuplicateMessage` 仅作为“无 client id 兼容兜底”，不作为主发送语义。
- 不再让 `ForceStatusForCompatibility`、实验性 dedupe 策略、legacy 状态桥接进入主链路决策。
- 为发送、callback、retry、summary 重算、mark read、sync 增加统一字段日志。
- 建立覆盖 P0/P1 风险的测试矩阵，补齐跨任务回归场景。

## 涉及的一致性问题

- 历史兼容逻辑再次改坏主链路。
- 线上排障困难。
- 后续开发缺少稳定回归防线。

## 独立验证方式

- `LegacyStatus` 覆盖列表展示，但不改变真实 message status。
- 兼容兜底未命中时，不会影响主发送幂等。
- 日志能串起 `request_id / user_id / conversation_id / message_id / attempt_id / device_id / cursor`。
- 回归测试覆盖重复发送、旧回调晚到、retry 冲突、mark read、sync ack 等关键场景。

## PR 边界建议

- 该 PR 主要是收敛和加固，不应再引入新的状态机语义变更。
- 如果发现前四个任务仍有业务语义未定，应回到对应任务修正，而不是在本任务里继续叠补丁。

## 风险隔离说明

- 本任务最后执行，可以避免“边改主链路边清理兼容”导致的问题定位困难。
- 该任务完成后，后续 AI Coding 开发可以直接复用日志字段和回归测试作为守护网。

## 已完成实现

- 已将 `LegacyStatus` 的影响范围收敛为列表展示层，不再影响主状态机和投影真相。
- 已保持 `FindLikelyDuplicateMessage` 仅作为无 `ClientMsgID` 兼容设备的兜底路径。
- 已在发送、callback、retry、mark read、sync 等主链路补充结构化字段日志，方便线上排障串联。
- 已补测试验证：
  - legacy display status 只影响列表展示
  - 真实 message status 不会被 legacy 字段覆盖
  - summary / unread 不会被 legacy 字段污染

---

## 任务间依赖总览

| 任务 | 风险等级 | 是否可独立提交 | 主要依赖 | 不应同时并发修改的核心逻辑 |
| --- | --- | --- | --- | --- |
| 消息发送幂等控制 | P0 | 是 | 无 | 发送创建主链路 |
| 发送状态机收敛与回退保护 | P0 | 是 | 任务 1 | attempt 状态机、retry 主链路 |
| 最近会话与未读真相重算 | P1 | 是 | 任务 2 | summary/unread/read state |
| 多设备同步确认式游标 | P0 | 是 | 任务 2 | sync cursor 与事件拉取 |
| 兼容逻辑收敛与排障能力补齐 | P1/P2 | 是 | 任务 1-4 | compat 使用边界、日志、回归测试 |

## 并发开发建议

- 任务 1 和任务 2 不建议并发，因为都会修改发送主链路和 repository 核心状态。
- 任务 3 和任务 4 可以在任务 2 完成后择一先做，但仍建议串行，降低 repository 冲突。
- 任务 5 必须最后做，因为它依赖前面所有业务语义已经稳定。

## 交付建议

- 每个任务都应自带最小必要测试，不把验证责任推给最后一个任务。
- 每个任务 PR 都应附上“本任务不解决什么”，防止范围蔓延。
- 每个任务合入后，都要保证 `demo/` 仍可跑通现有主链路与新增测试。
