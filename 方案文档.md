# demo/ 中等可靠性改造技术方案

## 1. 方案目标

本方案严格基于当前 `demo/` 做有限改造，不重写整体架构，目标是稳定以下核心问题：

1. 消息重复
2. 状态回退
3. 重试混乱
4. 多设备不一致
5. 最近会话列表错误
6. 未读数错误
7. 查询与排障困难

本方案的原则：

- 只收敛当前主链路，不引入理想化新架构
- 以 `internal/service` + `internal/repository` 为主改造面
- 把“消息真相”“attempt 真相”“summary/unread 投影”“device cursor”边界明确化
- 保留必要兼容逻辑，但不再让兼容逻辑污染主状态机
- 继续保持当前 `model -> repository -> service -> api` 的代码结构，不新增新的领域层、中间件层或异步框架
- 继续使用当前项目的 `log.Printf key=value` 风格，不引入新的日志框架
- 所有新增逻辑都必须有输入边界和幂等边界，避免为了修一致性问题引入明显的性能或安全风险

## 2. 当前主链路问题归因

当前问题主要来自 4 个根因：

1. 发送无幂等  
   当前主链路缺少稳定的原子幂等控制，重复点击或网络重放会产生重复消息。

2. attempt 回调可覆盖当前消息状态  
   回调只要拿到 `attemptID` 就能改消息状态，旧 attempt 的晚到回调会覆盖新状态。

3. summary/unread 是增量维护，不是基于真相重算  
   最近会话和未读只靠增量更新，遇到重试、回退、删除、已读时容易脏。

4. sync cursor 在“返回时推进”，而不是“客户端确认后推进”  
   设备没真正应用成功也会跳过事件，导致多设备长期不一致。

## 3. 改造范围

### 3.1 必改文件

- `demo/internal/service/message_service.go`
- `demo/internal/repository/message_repository.go`
- `demo/internal/model/message.go`
- `demo/internal/readmodel/read_state.go`
- `demo/internal/api/server.go`
- `demo/tests/public_test.go`

### 3.2 可复用但需收敛语义的文件

- `demo/internal/repository/compat_helpers.go`
- `demo/internal/compat/mobile_policy.go`

### 3.3 不进入主链路的文件

以下文件本次不接入主流程，只保留历史参考意义：

- `internal/experiments/*`
- `internal/legacy/*`
- `internal/provider/*`
- `internal/projection/*`
- `internal/syncengine/*`
- `internal/receipt/*`
- `internal/callback/*`

## 4. 核心设计结论

### 4.1 主状态真相

主状态真相只认：

- `Message.Status`
- `Message.ActiveAttemptID`
- `DeliveryAttempt.Status`
- `read_state.LastReadMessageID`
- `device_cursor.AckedCursor`

以下字段不再参与主状态机判断：

- `LegacyStatus`
- `compat/legacy/experiments` 中的状态映射 helper
- “回调最新时间即消息最新状态时间”这类历史语义

### 4.2 summary / unread 的真相来源

`ConversationSummary` 不再靠“增量加减”维护，改为：

- 每次关键状态变化后，只对受影响的 `userID + conversationID` 做局部重算
- 重算只扫描该会话消息，不扫描全量消息
- 未读数根据 `LastReadMessageID` 计算，不再只靠 `+1 / 清零`

### 4.3 sync cursor 的语义

- `next_cursor` 只是“服务端本次返回到哪里”
- 真正持久化的设备进度，必须是客户端确认过的 `ack_cursor`

### 4.4 风格与边界

- 不新增新的同步引擎、投影子系统、事件总线或后台 worker
- 不改变当前内存仓储的技术选型，只在当前 `MemoryMessageRepository` 内补齐索引和事务边界
- 安全边界只做“输入校验 + 日志脱敏 + 幂等保护”，不引入完整鉴权系统

## 5. 数据结构变更

### 5.1 `internal/service/message_service.go`

`SyncRequest` 新增：

- `AckCursor int64`

语义：

- `Cursor`：本次拉取起点
- `AckCursor`：客户端已经成功应用的最大 cursor

同时补充输入约束：

- `UserID > 0`
- `DeviceID` 非空
- `Cursor >= 0`
- `AckCursor >= 0`
- `Limit <= 100`

说明：

- 这些校验必须在 `service` 或 `api` 层完成，保持当前项目“参数校验靠上层，状态更新靠 repository”的风格
- 不新增独立 validator 包

### 5.2 `internal/model/message.go`

`SyncEvent` 建议新增字段：

- `MessageVersion int64`
- `SummaryUnreadCount int`
- `SummaryLastMessageID int64`
- `ReadMessageID int64`

用途：

- 多端客户端即使重复收到同一消息的不同事件，也可以基于 `Seq + MessageVersion` 做更稳的本地更新
- 服务端日志排障时可直接看到消息版本推进
- 已读和最近会话变化复用现有 `/sync` 事件流，不单独再造一套接口

`ConversationSummary`、`Message`、`DeliveryAttempt` 结构尽量不做大改，只补充最小必要字段。

### 5.3 `internal/readmodel/read_state.go`

将该文件中的 `ReadState` 正式接入仓储使用。

服务端持久化维度采用：

- `UserID`
- `ConversationID`
- `LastReadMessageID`
- `UpdatedAt`

本期只把 `LastReadMessageID` 作为未读真相源，不扩展新的已读模型。

### 5.4 `internal/repository/message_repository.go` 内存结构新增

在 `MemoryMessageRepository` 中新增：

- `readStates map[string]readmodel.ReadState`
- `clientMsgIndex map[string]int64`
- `conversationMessages map[int64][]int64`
- `attemptsByMsg map[int64][]int64`

用途：

- `readStates`：未读重算
- `clientMsgIndex`：发送幂等查询
- `conversationMessages`：只扫描单会话消息，避免 summary/list 每次扫全表
- `attemptsByMsg`：快速定位消息 attempt 列表，避免每次扫全 attempts

初始化同步修改 `NewMemoryMessageRepository()`。

## 6. Repository 接口变更

在 `MessageRepository` 增加或调整以下方法：

- `SendMessage(msg model.Message, dedupeWindow time.Duration) (model.Message, model.DeliveryAttempt, bool, string, error)`
- `FindByClientMsgID(senderID int64, conversationID int64, clientMsgID string) (model.Message, error)`
- `GetAttempt(id int64) (model.DeliveryAttempt, error)`
- `GetActiveOrLatestAttempt(messageID int64) (model.DeliveryAttempt, error)`
- `RetryMessage(messageID int64) (model.Message, model.DeliveryAttempt, error)`
- `AckDeviceCursor(userID int64, deviceID string, cursor int64) int64`

保留但调整使用方式的方法：

- `CreateMessage`：保留为 repository 内部 helper，不再作为发送主流程的外部编排步骤
- `StartAttempt`：保留为 repository 内部 helper，不再由 service 主流程拆分调用
- `SaveMessage`：不再用于 retry 主流程
- `SaveDeviceCursor`：不再由 `Sync` 直接调用推进 returned cursor
- `SetLegacyDisplayStatus`：只改 `LegacyStatus`，不影响主状态
- `FindLikelyDuplicateMessage`：仅用于“无 ClientMsgID 的兼容兜底”，不作为主方案

## 7. 主链路改造细节

### 7.1 发送链路

修改文件：

- `internal/service/message_service.go`
- `internal/repository/message_repository.go`

新流程：

1. `service.SendMessage` 只负责参数校验、兼容策略判断和日志。
2. service 将 `model.Message + dedupeWindow` 一次性交给 repository 的原子方法处理。
3. repository 在同一把锁内完成：
   - `ClientMsgID` 命中幂等查询
   - 无 `ClientMsgID` 时的兼容短窗口内容去重
   - 创建 message
   - 创建首个 attempt
   - 更新 sender 事件与 sender summary
4. 若命中幂等，直接返回已有 message 和 active/latest attempt。
5. 若未命中幂等，返回新建的 message 和新 attempt。

实现要求：

- 新增仓储原子入口，例如 `SendMessage(msg, dedupeWindow)`
- `CreateMessage` 和 `StartAttempt` 可以保留，但仅作为 repository 内部 helper 使用
- `CreateMessage` 时建立 `clientMsgIndex`
- `conversationMessages[conversationID]` 追加 `messageID`
- `StartAttempt` 时写 `attemptsByMsg[messageID]`
- 返回已有消息时，attempt 必须返回当前活跃 attempt；若不存在活跃 attempt，返回最新 attempt

设计说明：

- 主幂等键：`senderID + conversationID + clientMsgID`
- 不采用“纯文本相似度智能去重”作为主逻辑
- 保留兼容兜底，但只在 `ClientMsgID` 缺失时启用
- 幂等判断与创建必须在单次仓储调用内完成，严禁 service 层先查再写，否则并发下仍会重复建消息
- 对 `Content` 建议补充长度上限，例如 4KB，避免 demo 被异常请求拖垮内存或日志

### 7.2 回调完成链路

修改文件：

- `internal/service/message_service.go`
- `internal/repository/message_repository.go`

新规则：

`CompleteAttempt(attemptID, success, errorCode)` 必须满足以下规则：

1. attempt 不存在：返回错误
2. attempt 已经完成：
   - 如果结果相同：视为回调重放，直接返回当前消息，不重复写事件、不重复改 summary、不重复加未读
   - 如果结果不同：视为异常重放，忽略消息状态变更，记录告警日志
3. 如果 `message.ActiveAttemptID != attemptID`：
   - 可以把 attempt 自身状态更新为完成
   - 但不得再覆盖 `Message.Status`
   - 不得写新的 message sync event
   - 不得触发 summary/unread 变更
4. 只有当 `attemptID == message.ActiveAttemptID` 时，才允许更新 `Message.Status`
5. `deleted` 消息优先级最高：
   - 如果消息已删除，任何回调都不能再恢复状态

状态推进规则：

- `sending -> sent`
- `sending -> failed`
- `failed -> sending` 仅允许通过 retry 主流程
- `sent` 不允许被旧 attempt 改回 `failed`
- `deleted` 不允许被任何回调改回其他状态

Side effect 要求：

当且仅当主消息状态实际发生变化时：

- 给发送方写 `message_updated` 事件
- 若接收方可见性发生变化，也给接收方写 `message_updated` 事件
- 重算发送方 summary
- 重算接收方 summary

### 7.3 重试链路

修改文件：

- `internal/service/message_service.go`
- `internal/repository/message_repository.go`

需要删除/收敛的逻辑：

当前 retry 不能再采用“service 先改 message，再开 attempt”的两段式流程。

新流程：

新增仓储原子方法 `RetryMessage(messageID)`，在同一把锁内完成：

1. 读取 message
2. 校验当前状态
3. 判断是否已存在正在运行的 `ActiveAttemptID`
4. 决定是否幂等返回已有 attempt
5. 创建新 attempt
6. 设置 `ActiveAttemptID`
7. 设置 `Message.Status=sending`
8. 更新版本与时间
9. 写发送方 sync event
10. 重算发送方 summary

重试规则：

- `failed`：允许 retry
- `sending`：如果当前 active attempt 仍在 running，则直接返回当前 message + active attempt，视为 retry 幂等
- `sent`：不允许 retry，返回业务错误
- `deleted`：不允许 retry，返回业务错误

接收方可见性规则：

- retry 导致的 `failed -> sending` 只对发送方可见
- 不给接收方推送“对方又开始 sending 了”的状态

## 8. summary / unread 改造

### 8.1 修改文件

- `internal/repository/message_repository.go`
- `internal/readmodel/read_state.go`

### 8.2 删除/收敛的逻辑

以下逻辑不再作为 summary/unread 主维护方式：

- `updateSummaryLocked(... incrementUnread bool)`
- `UnreadCount++`
- `MarkConversationRead()` 里只清零 summary 的做法

`updateSummaryLocked` 可以保留，但只作为兼容 wrapper，内部统一走重算。

### 8.3 新增内部方法

在 repository 中新增：

- `rebuildSummaryLocked(userID int64, conversationID int64)`
- `computeUnreadLocked(userID int64, conversationID int64) int`
- `lastVisibleMessageLocked(userID int64, conversationID int64) (model.Message, bool)`
- `messageVisibleToUser(msg model.Message, userID int64) bool`

### 8.4 可见性规则

对发送方：

- 非删除消息都可见
- `sending/failed/sent` 都可见

对接收方：

- 只有 `sent` 且未删除的消息可见

### 8.5 summary 重算规则

重算 `ConversationSummary` 时：

1. 从 `conversationMessages[conversationID]` 取消息集合
2. 根据 `messageVisibleToUser` 过滤
3. 找最后一条可见消息
4. 更新：
   - `LastMessageID`
   - `LastMessagePreview`
   - `UpdatedAt`
5. 未读数按 `LastReadMessageID` 重新计算

性能约束：

- 只允许重算“受影响会话 + 受影响用户”
- 不允许因为一个消息状态变化而扫描全局 `messages`
- 对“无状态变化”的回调重放，不做 summary 重算

### 8.6 unread 计算规则

对某个 `userID + conversationID`：

- 统计所有：
  - `ReceiverID == userID`
  - `Status == sent`
  - `ID > LastReadMessageID`
  - 未删除
- 得到 `UnreadCount`

### 8.7 MarkConversationRead 新语义

`MarkConversationRead(userID, conversationID)` 改为：

1. 找该用户在该会话下最后一条“对其可见”的接收消息
2. 将 `readStates[key].LastReadMessageID` 设为该消息 ID
3. 重算 summary
4. 不改 `Message.Status`
5. 不制造假的 `message_updated` 事件
6. 追加一条 `summary_updated` 事件进入现有 `/sync` 事件流

强约束：

- 已读变化必须能被同一用户其他设备通过 `/sync` 感知
- 本期不新增新的 HTTP 已读同步接口，只在现有 `SyncEvent` 中追加 `summary_updated` 事件

## 9. sync cursor 改造

### 9.1 修改文件

- `internal/service/message_service.go`
- `internal/repository/message_repository.go`
- `internal/api/server.go`

### 9.2 需要收敛的逻辑

收敛掉当前逻辑：

- `Sync()` 返回事件后立即 `SaveDeviceCursor(nextCursor)`

这个行为必须删除。

### 9.3 新流程

`Sync(req)` 执行顺序：

1. 如果 `req.AckCursor > 0`：
   - 调 `AckDeviceCursor(userID, deviceID, ackCursor)`
   - 使用 `max(old, ackCursor)` 保证单调推进
2. 计算本次拉取起点：
   - 若 `req.Cursor > 0`，优先使用请求 cursor
   - 否则使用已 ack 的设备 cursor
3. 调 `ListEventsAfter(userID, cursor)`
4. 截断 `Limit`
5. 计算 `nextCursor`
6. 返回 `events + nextCursor`
7. 不持久化 `nextCursor`

### 9.4 API 兼容

HTTP `/sync` 新增可选 query 参数：

- `ack_cursor`

旧客户端不传时仍可工作：

- 只是退化成“显式 cursor 拉取”
- 不再隐式把 returned cursor 视为已应用

### 9.5 事件内容要求

`appendEventLocked` 写事件时应补齐：

- `MessageVersion`
- 当前 `MessageStatus`
- 已读 / summary 相关字段

### 9.6 输入校验与安全边界

`/sync` 和 `/messages/send` 必须补齐最小输入边界，避免把 demo 做成“能跑但谁都能把状态打脏”：

- `/messages/send`
  - `sender_id/receiver_id/conversation_id > 0`
  - `content` 去首尾空白后不能为空
  - `content` 需要长度上限
- `/sync`
  - `user_id > 0`
  - `device_id` 非空
  - `cursor/ack_cursor >= 0`
  - 非法参数返回 `400`，而不是静默按 0 处理

说明：

- 这是有限但必要的安全边界，不引入鉴权系统，也不引入复杂 ACL
- 当前 demo 没有完整登录态，本期只做参数级防御和状态级幂等保护

## 10. 列表读取逻辑改造

### 10.1 修改文件

- `internal/repository/message_repository.go`
- `internal/service/message_service.go`

### 10.2 改造点

`ListConversationMessages(userID, conversationID, offset, limit)`：

- 不再扫描 `r.messages` 全表
- 使用 `conversationMessages[conversationID]`
- 读取时根据 `userID` 做可见性过滤
- 仍保留 `LegacyStatus` 覆盖展示状态的兼容行为
- 但只影响返回列表展示，不影响真实 `Message.Status`

### 10.3 兼容保留说明

保留：

- `LegacyStatus` 用于老列表展示兼容
- `Version += attemptCount` 的历史 UI 兼容逻辑可暂时保留

收敛：

- `LegacyStatus` 不再参与：
  - attempt 完成逻辑
  - summary 计算
  - unread 计算
  - sync 事件

## 11. 需要保留 / 删除 / 隔离的逻辑

### 11.1 保留但缩小作用域

- `SetLegacyDisplayStatus()`
  - 只改 `LegacyStatus`，不触发 summary/unread/sync
- `FindLikelyDuplicateMessage()`
  - 仅用于 `ClientMsgID` 为空时的兼容兜底
- `compat/mobile_policy.go`
  - 仅用于决定是否启用“无 client id 时的短窗口兜底去重”

### 11.2 不再进入主流程

- `ForceStatusForCompatibility()`
- `ApplyProjectionMessageStatus()`
- `FastForwardDeviceCursor()`
- `experiments/feature_flags.go` 中的 flag 逻辑
- `experiments/dedupe_strategies.go` 中的评分逻辑
- `legacy/status_bridge.go` 中的主状态映射逻辑

这些文件不要求删除，但主服务不得再依赖它们决策消息真相。

## 12. 锁、事务、幂等控制要求

### 12.1 锁边界

当前内存仓储已有全局 `mu sync.Mutex`，本期继续使用它作为事务边界。

要求：

- 所有会同时修改 `messages + attempts + events + summaries + readStates + deviceCursors` 的操作，都必须在同一 repo 方法内完成
- service 层不得把“半个状态变更”拆成多个 repo 调用完成
- 不引入更细粒度锁或并发容器，避免为了 demo 过度设计

### 12.2 必须原子化的方法

以下方法必须在单锁内原子执行：

- `SendMessage`
- `CompleteAttempt`
- `RetryMessage`
- `MarkConversationRead`
- `AckDeviceCursor`

说明：

- `CreateMessage` / `StartAttempt` 可以保留，但不再作为发送主流程对外组合调用
- 原子化的重点是“业务入口方法”，不是把每个小 helper 都公开成事务边界

### 12.3 幂等控制

发送幂等：

- `senderID + conversationID + clientMsgID`

回调幂等：

- `attemptID`
- 同一 attempt 首次 terminal 结果生效
- 后续重复回调只记录日志

retry 幂等：

- 同一 message 若当前 active attempt 仍在 running，重复 retry 返回当前 attempt，不创建新 attempt

cursor 幂等：

- `AckDeviceCursor` 只能单调前进，不能回退

## 13. 必须新增的日志

日志继续使用 `log.Printf("event=... key=value")` 风格，不引入新的日志依赖。

安全约束：

- 不记录消息正文 `content`
- 不直接记录第三方回调原始 payload
- `error_code` 可记录，原始错误对象仅记录必要摘要
- 避免把用户输入原样拼进日志，防止日志污染和排障噪音

### 13.1 发送日志

在 `SendMessage` 增加：

- `request_id`
- `sender_id`
- `receiver_id`
- `conversation_id`
- `device_id`
- `client_msg_id`
- `message_id`
- `active_attempt_id`
- `dedupe_hit`
- `dedupe_reason`

关键日志点：

- `send_rejected`
- `send_deduped`
- `send_message_created`
- `send_attempt_started`

### 13.2 回调日志

在 `CompleteAttempt` 增加：

- `attempt_id`
- `message_id`
- `active_attempt_id`
- `success`
- `error_code`
- `ignore_reason`

关键日志点：

- `callback_failed`
- `callback_replayed_same_result`
- `callback_replayed_conflicting_result`
- `callback_ignored_stale_attempt`
- `callback_applied_status_change`

### 13.3 重试日志

关键日志点：

- `retry_failed`
- `retry_return_running_attempt`
- `retry_completed`

字段：

- `message_id`
- `old_attempt_id`
- `new_attempt_id`
- `old_status`
- `new_status`

### 13.4 summary / read 日志

关键日志点：

- `summary_rebuilt`
- `conversation_marked_read`

字段：

- `user_id`
- `conversation_id`
- `last_message_id`
- `unread_count`
- `last_read_message_id`

### 13.5 sync 日志

关键日志点：

- `sync_failed`
- `sync_completed`

字段：

- `user_id`
- `device_id`
- `request_cursor`
- `ack_cursor`
- `effective_cursor`
- `next_cursor`
- `event_count`

## 14. 测试补齐方案

### 14.1 修改文件

- 优先扩展 `demo/tests/public_test.go`
- 如内容过多，可拆新文件：
  - `tests/send_dedupe_test.go`
  - `tests/attempt_state_test.go`
  - `tests/summary_unread_test.go`
  - `tests/sync_cursor_test.go`

### 14.2 必补用例

发送幂等：

- 同一 `client_msg_id` 重复发送，只创建一条 message
- 同一 `client_msg_id` 并发发送，只存在一个活跃 attempt
- 无 `client_msg_id` 时，非兼容设备不做内容去重
- 无 `client_msg_id` 时，兼容设备短窗口内命中兜底去重

回调防回退：

- active attempt 成功后，旧 failed callback 不得把消息改回 failed
- active attempt 失败后，旧 success callback 不得再次覆盖
- 同一 attempt 重复 success callback，不得重复写 unread / summary side effect

retry 语义：

- failed 消息 retry 生成新 attempt
- sending 消息重复 retry 返回当前 running attempt
- sent 消息 retry 返回错误
- retry 后旧 attempt 回调到达，不得覆盖新状态

summary / unread：

- 发送方 summary 在消息创建时可见 sending
- 接收方 summary 只有成功 sent 后才可见
- `MarkConversationRead` 后 unread 为 0
- 新消息 sent 后 unread 按 `LastReadMessageID` 正确递增
- 状态回退被拦截后，unread 不应脏增量

sync cursor：

- `Sync` 返回 `next_cursor` 但不自动推进持久 cursor
- `AckCursor` 提交后，下次不再返回已 ack 事件
- `AckCursor` 回退请求不生效
- 多次重复 ack 同一个 cursor 幂等
- 非法 `user_id/device_id/cursor` 返回 `400`

列表兼容：

- `LegacyStatus` 可覆盖列表展示
- 但 `GetMessage` 真实状态不变
- `LegacyStatus` 不影响 unread 与 summary

安全与边界：

- 超长 `content` 被拒绝
- `/sync` 缺失必填参数被拒绝
- 日志不包含消息正文

## 15. 实施顺序

建议按 4 个小阶段实施，降低风险。

### 阶段 1：数据结构与索引

修改：

- `model/message.go`
- `readmodel/read_state.go`
- `repository/message_repository.go`

完成：

- 新 map 初始化
- `GetAttempt`
- `GetActiveOrLatestAttempt`
- `clientMsgIndex`
- `conversationMessages`
- `attemptsByMsg`
- `readStates`

### 阶段 2：发送 / 回调 / 重试主状态机

修改：

- `service/message_service.go`
- `repository/message_repository.go`

完成：

- 原子发送幂等
- attempt 防回退
- retry 原子化
- stale callback 忽略
- 输入长度和参数校验

### 阶段 3：summary / unread / read state

修改：

- `repository/message_repository.go`

完成：

- `rebuildSummaryLocked`
- `MarkConversationRead` 新语义
- receiver visibility 规则
- 删除旧的增量 `updateSummaryLocked` 依赖
- `summary_updated` 进入现有 sync 事件流

### 阶段 4：sync cursor 与日志测试

修改：

- `service/message_service.go`
- `api/server.go`
- `tests/*`

完成：

- `AckCursor`
- sync 不再自动持久化 returned cursor
- 补参数校验
- 补日志
- 补完整测试

## 16. 风险控制说明

本方案的风险控制点：

- 不改 HTTP 主路由，只做可选参数扩展
- 不重写存储层，仍用单 repository + 单 mutex
- 不引入消息队列、事件总线、CQRS、分布式事务
- 不让 `legacy/experiments` 代码重新进入主逻辑
- 先稳住状态真相，再重算 summary/unread，不做“边改边猜”
- summary/unread 的重算只允许发生在“受影响会话 + 受影响用户”范围内，不能退化成全局重扫
- 对回调重放、非法参数、超长 content、cursor 回退都要有明确拒绝或忽略策略

## 17. 与当前代码风格保持一致的约束

为了保证改造后代码和当前 `demo/` 风格一致，开发时必须遵守：

- 继续沿用当前 `internal/model`、`internal/repository`、`internal/service`、`internal/api` 的四层分工
- 不新增新的顶层模块来承载状态机、同步引擎或投影系统
- repository 继续使用内存 map + `sync.Mutex`，不引入 channel、worker、后台 goroutine
- service 继续做轻量参数处理、兼容策略判断和日志，不在 service 层保存状态
- api 层只做 HTTP 参数解析和错误返回，不把业务规则下沉到 handler
- 测试继续以 `demo/tests/public_test.go` 为主，优先补集成式行为测试，而不是先拆很多测试基础设施
- 兼容逻辑保留最小必要面，不新增新的“实验策略开关”或“迁移桥接层”

## 18. 最终落地标准

开发完成后，系统应满足：

1. 重复点击发送不会产生多条重复消息
2. 旧回调不会把新状态覆盖掉
3. retry 不会生成状态混乱
4. 最近会话列表与消息真实状态长期一致
5. 未读数可通过 read state 重算，不依赖脆弱增量
6. sync cursor 只在客户端确认后推进
7. 已读和最近会话变化能通过现有 `/sync` 事件流传到同一用户其他设备
8. 日志能串起 `user/device/conversation/message/attempt/cursor`，且不泄露消息正文
